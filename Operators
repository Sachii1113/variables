/**
 * There are
 * arthematic operators  +,-,*,/,%,**,%,++,--
 * assignmentOperators =,arthematic operators=
 * comparationOperators ==,===,>=,<=,!=,>,<,?
 * 
 */
let i = 12
let j = myFunction(i)
console.log(j);
function myFunction(i)
{
     i+=23
    return i
}
